<!-- questions.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Questions</title>
    <!-- Add Bootstrap CSS link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="container mt-5">
    <h1 class="mb-4">Welcome to Questions Page</h1>

    <ul class="list-group">
        <li class="list-group-item">
            <h2>
                <%= question.title %>
            </h2>
            <ul class="list-unstyled">
                <% resultats.forEach(respuesta => { %>
                    <li>
                        <form action="/check-answer" method="post">
                            <input type="hidden" name="questionId" value="<%=question.id%>">
                            <button type="submit" name="optionId" value="<%= question.option1 %>"
                                class="btn btn-primary">
                                <%= respuesta%>
                            </button>
                        </form>
                    </li>
                <% }); %>
            <!--                
                <li>
                    <form action="/check-answer" method="post">
                        <input type="hidden" name="questionId" value="<%= question.id %>">
                        <button type="submit" name="optionId" value="<%= question.option1 %>" class="btn btn-primary">
                            <%= question.option1 %>
                        </button>
                    </form>
                </li>
                <li>
                    <form action="/check-answer" method="post">
                        <input type="hidden" name="questionId" value="<%= question.id %>">
                        <button type="submit" name="optionId" value="<%= question.option2 %>" class="btn btn-primary">
                            <%= question.option2 %>
                        </button>
                    </form>
                </li>
                <li>
                    <form action="/check-answer" method="post">
                        <input type="hidden" name="questionId" value="<%= question.id %>">
                        <button type="submit" name="optionId" value="<%= question.option3 %>" class="btn btn-primary">
                            <%= question.option3 %>
                        </button>
                    </form>
                </li>
            -->
            </ul>
        </li>
    </ul>

    <!-- Add Bootstrap JS and Popper.js scripts (required for some Bootstrap components) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>

</html>